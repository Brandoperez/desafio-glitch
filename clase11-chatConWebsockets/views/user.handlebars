<h1>Hola, {{usuario.nombre}}</h1>

{{#if isTutor}}
    {{#each cursos}}

        <div>
            <p>{{this.numCurso}} {{this.horario}} {{this.dia}}</p>
        </div>
    
    {{/each}}
    {{else}}
        <p>No es un tutor</p>
        {{/if}}